name: Publish main branch to npm tag "dev" (deprecated)

on:
  workflow_dispatch:

jobs:
  moved:
    runs-on: ubuntu-latest
    steps:
      - name: Explain migration
        run: |
          echo "::error title=Workflow moved::Dev publish now runs from .github/workflows/package.yml because npm Trusted Publishers allow one workflow filename per package."
          exit 1
